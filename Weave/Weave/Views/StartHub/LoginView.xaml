<UserControl
    x:Class="Weave.Views.StartHub.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Weave.Views.StartHub"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:common="using:Weave.Common"
    mc:Ignorable="d"
    Width="360"
    Height="500"
    VerticalAlignment="Top"
    >
    
    <UserControl.Resources>

        <common:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        
        <Style TargetType="TextBlock" x:Key="AccountHeaderStyle">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="26.667" />
            <Setter Property="Margin" Value="0,-5,0,-7" />
        </Style>
        
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Header}" Style="{StaticResource SubheaderTextStyle}" />
        <Grid Background="#2B2B2B" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Background="#474747">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="&#xE117;" FontFamily="Segoe UI Symbol" VerticalAlignment="Center" Margin="15" FontSize="50" Foreground="#272727" />
                <TextBlock Grid.Column="1" Text="Login via one of the services below if you'd like to sync your feeds and preferences on another device" TextWrapping="Wrap" Foreground="{StaticResource SubtleTextBrush}" FontSize="15" Margin="0,20,20,20" />
            </Grid>
            <ItemsControl Margin="40,30,40,40" Grid.Row="1" ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ButtonStyleBasic}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="0,10,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Rectangle Fill="{StaticResource ImagePlaceholderBrush}" Width="50" Height="50" Grid.RowSpan="2" Margin="0,0,20,0" />
                                <TextBlock Text="{Binding Header}" Style="{StaticResource AccountHeaderStyle}" Grid.Column="1" />
                                <Grid Grid.Column="1" Grid.Row="1">
                                    <TextBlock Text="{Binding LoggedOutText}" VerticalAlignment="Bottom" Foreground="{StaticResource SubtleTextBrush}" />
                                </Grid>
                                <Button Grid.Column="2" Grid.RowSpan="2" Style="{StaticResource ButtonStyleBasic}" Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="&#xE115;" FontFamily="Segoe UI Symbol" VerticalAlignment="Center" Foreground="{StaticResource SubtleTextBrush}" />
                                </Button>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
